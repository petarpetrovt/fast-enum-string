namespace FastEnumString.Tests
{
	using System;
	using System.Linq;
	using Microsoft.VisualStudio.TestTools.UnitTesting;

	[TestClass]
	public class FastEnumStringTests
	{
		[TestMethod]
		public void Test_ToStringFast()
		{
			//var expectedValues = Enum
			//	.GetValues(typeof(TestEnum))
			//	.Cast<TestEnum>()
			//	.Select(x => x.ToString())
			//	.ToArray();
			//var actualValues = Enum
			//	.GetValues(typeof(TestEnum))
			//	.Cast<TestEnum>()
			//	.Select(x => x.ToStringFast())
			//	.ToArray();

			//CollectionAssert.AreEqual(expectedValues, actualValues);
		}
	}
}
